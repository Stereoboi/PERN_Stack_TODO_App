/* eslint-disable import/no-cycle */
import React from 'react';

import { RegisterForm } from '../../common/components/registration-form';

const MyProfileContainer = () => {
  return <RegisterForm />;
};

export default MyProfileContainer;
